# Pinu 能量系统群内机器人提数指南

更新日期：2026-06-22

## 1. 这次先看什么

你现在只想先回答三个基础问题：

1. 实际用户每天消耗了多少能量。
2. 实际用户每天补充了多少能量。
3. 其中手动补充了多少能量。

以及一个转化问题：

4. 能量耗尽是否影响订阅转化。

建议先不要一次性追太多复杂结论，先让数据机器人跑出一张“用户日级能量收支表”，再基于这张表继续拆订阅、留存和分场景。

## 2. 最推荐的数据口径

### 2.1 最可靠口径：后端能量流水

如果后端有能量余额变化流水，这是最优先的数据源。

需要字段：

| 字段 | 用途 |
|---|---|
| `dt` | 日期 |
| `cuid / uid` | 用户标识 |
| `change_time` | 余额变化时间 |
| `before_balance` | 变化前余额 |
| `after_balance` | 变化后余额 |
| `change_value` | 本次变化值，正数为补充，负数为消耗 |
| `change_reason` | 变化原因 |
| `is_subscribe` | 是否在期会员 |
| `user_type` | new / old |
| `os` | iOS / Android |
| `app_version` | App 版本 |

推荐的 `change_reason` 分类：

| 分类 | 说明 |
|---|---|
| `question_deduct` | 做题消耗 |
| `auto_recover` | 自动恢复 |
| `method_A` | 能量页方式 A 领取 |
| `method_B` | 耗尽弹窗方式 B 领取 |
| `combo_reward` | 连对奖励 |
| `full_correct_reward` | 整卷全对奖励 |
| `subscription_switch` | 订阅状态切换相关，不建议混入普通补能 |
| `system_adjust` | 系统修正或异常补偿，需要单独列出 |

### 2.2 如果没有后端流水：用埋点近似

现有埋点能近似回答部分问题：

| 问题 | 可用埋点 | 可信度 |
|---|---|---|
| 手动方式 A 补充 | `KDM_007`，过滤 `claimStatus=1`，取 `rewardAmount` | 中高 |
| 手动方式 B 补充 | `KDM_013`，过滤 `claimStatus=1`，取 `rewardAmount` | 中高 |
| 连对/全对奖励 | `KDM_008`，取 `rewardAmount` 和 `comboCount` | 中 |
| 自动恢复补充 | 现有前端埋点不一定完整 | 低，建议走后端流水 |
| 做题消耗 | 需要答题明细或后端扣减流水 | 中高，取决于表是否能排除跳过/错题重练 |

注意：`KDM_001/KDM_003` 里的 `powerAmount` 是曝光时余额快照，不适合直接当作补能流水。

## 3. 群内机器人提数请求 1：每日能量收支总表

这条最重要，先发它。

```text
@新业务数据助手 请帮我查询 Pingo 能量系统 2026-06-11 到 2026-06-17 非在期会员用户的每日能量收支。

优先口径：
请优先使用后端能量余额变化流水表。如果没有后端能量流水，请说明没有，并尝试用埋点和答题明细近似。

用户范围：
1. 非在期会员用户。
2. 按 dt、user_type(new/old)、os 拆分。
3. 如果有 app 版本，请按用户最近一次活跃版本归属，只看能量系统上线的新版本用户。

输出指标：
1. 非在期会员活跃 uv
2. 有效学习用户 uv
3. 消耗能量总量
4. 人均消耗能量 = 消耗能量总量 / 非在期会员活跃 uv
5. 学习用户人均消耗能量 = 消耗能量总量 / 有效学习用户 uv
6. 补充能量总量
7. 人均补充能量 = 补充能量总量 / 非在期会员活跃 uv
8. 学习用户人均补充能量 = 补充能量总量 / 有效学习用户 uv
9. 净能量差 = 补充能量总量 - 消耗能量总量
10. 人均净能量差 = 人均补充能量 - 人均消耗能量

补充能量请拆分来源：
1. 自动恢复补充能量
2. 方式 A 领取补充能量
3. 方式 B 领取补充能量
4. 连对奖励补充能量
5. 整卷全对奖励补充能量
6. 其他/系统修正补充能量

消耗能量请拆分来源：
1. 做题消耗能量
2. 其他/系统修正消耗能量

注意：
1. 做题消耗要排除跳过题和错题重练，因为 PRD 中跳过和错题重练不扣能量。
2. 方式 A/方式 B 只统计领取成功，不要把点击失败算入补充。
3. 如果某个来源无法统计，请在结果里单独标记“缺数据源/缺字段”。
```

## 4. 群内机器人提数请求 2：手动补充能量

如果你只想先看手动补充，这条可以单独发。

```text
@新业务数据助手 请帮我查询 Pingo 能量系统 2026-06-11 到 2026-06-17 非在期会员用户手动补充能量情况。

用户范围：
1. 非在期会员用户。
2. 按 dt、user_type(new/old)、os 拆分。
3. 如能识别新版本用户，请只看能量系统上线的新版本用户。

事件口径：
1. 方式 A：KDM_007，能量页面定时领取点击，claimStatus=1 视为领取成功。
2. 方式 B：KDM_013，耗尽弹窗福利领取按钮点击，claimStatus=1 视为领取成功。

输出指标：
1. 非在期会员活跃 uv
2. 方式 A 领取成功 uv
3. 方式 A 领取成功次数
4. 方式 A 补充能量总量，使用 rewardAmount 汇总
5. 方式 A 人均补充能量 = 方式 A 补充能量总量 / 非在期会员活跃 uv
6. 方式 B 领取成功 uv
7. 方式 B 领取成功次数
8. 方式 B 补充能量总量，使用 rewardAmount 汇总
9. 方式 B 人均补充能量 = 方式 B 补充能量总量 / 非在期会员活跃 uv
10. 手动补充总量 = 方式 A 补充能量总量 + 方式 B 补充能量总量
11. 手动补充用户占比 = 方式 A 或方式 B 领取成功 uv / 非在期会员活跃 uv

请额外输出：
1. 按领取次数分布：0 次、1 次、2 次、3 次、4 次及以上。
2. 方式 B 中 isNew=1 和 isNew=2 的拆分。
3. claimStatus 非成功的 pv/uv，帮助判断失败或灰态点击情况。
```

## 5. 群内机器人提数请求 3：补充 vs 消耗是否过宽

这条用于判断“能量是不是太充足”。

```text
@新业务数据助手 请基于每日能量收支结果，帮我查询 2026-06-11 到 2026-06-17 Pingo 非在期会员用户的能量供需分布。

用户范围：
非在期会员、新版本用户，按 dt、user_type(new/old)、os 拆分。

用户日粒度先计算：
1. daily_consumed_power：用户当天消耗能量
2. daily_gained_power：用户当天补充能量
3. daily_manual_gained_power：用户当天手动补充能量，即方式 A + 方式 B
4. daily_net_power = daily_gained_power - daily_consumed_power

输出指标：
1. 用户数
2. 人均消耗能量
3. 人均补充能量
4. 人均手动补充能量
5. daily_net_power > 0 的用户占比
6. daily_net_power = 0 的用户占比
7. daily_net_power < 0 的用户占比
8. 消耗能量 P50/P75/P90/P95/P99
9. 补充能量 P50/P75/P90/P95/P99
10. 手动补充能量 P50/P75/P90/P95/P99
11. 耗尽用户 vs 未耗尽用户分别的人均消耗、人均补充、人均手动补充

判断目的：
如果高学习用户 daily_net_power 仍然 >= 0，说明能量供给可能偏宽。
如果大量用户 daily_net_power < 0 且耗尽后流失，说明能量可能偏紧。
```

## 6. 能否根据现有埋点看“能量耗尽对订阅转化的影响”

可以做，但要分清楚两件事：

1. **可以做转化漏斗和相关性分析**：能量耗尽弹窗曝光后，有多少人点击订阅、进入订阅页、订阅成功。
2. **不能直接证明因果影响**：如果没有 A/B test 或准实验，只能说“耗尽用户的订阅转化表现”，不能严格说“耗尽导致了订阅提升”。

### 6.1 现有埋点可以串的链路

| 环节 | 事件 | 关键字段 |
|---|---|---|
| 能量耗尽弹窗曝光 | `KDM_009` | `scene`, `isNew` |
| 耗尽弹窗订阅 CTA 展示 | `KDM_010` | `scene` |
| 耗尽弹窗订阅按钮点击 | `KDM_011` | `scene` |
| 订阅页展示 | `KDS_001` | `enterFrom=4` 表示能量耗尽弹窗 |
| 订阅页按钮点击 | `KDS_003` | `enterFrom=4` |
| 订阅成功展示 | `KDT_001` | `enterFrom=4` |

主要风险：

- `KDM_009/KDM_010/KDM_011/KDM_014` 的 `scene` 字段在埋点文档中标注“scene 不对”，所以分场景分析前必须先校验。
- `enterFrom=4` 可以帮助归因“来自能量耗尽弹窗”，但最好再看用户级时序：订阅成功是否发生在耗尽弹窗后 24 小时内。

## 7. 群内机器人提数请求 4：耗尽到订阅漏斗

```text
@新业务数据助手 请帮我查询 Pingo 能量系统 2026-06-11 到 2026-06-17 非在期会员用户的能量耗尽到订阅转化漏斗。

用户范围：
1. 非在期会员用户。
2. 按 dt、user_type(new/old)、os 拆分。
3. 如能识别新版本用户，请只看能量系统上线的新版本用户。

漏斗事件：
1. KDM_009：能量耗尽弹窗曝光 uv
2. KDM_010：耗尽弹窗订阅 CTA 展示 uv
3. KDM_011：耗尽弹窗订阅按钮点击 uv
4. KDS_001：订阅落地页展示 uv，过滤 enterFrom=4
5. KDS_003：订阅页底部订阅按钮点击 uv，过滤 enterFrom=4
6. KDT_001：订阅成功展示 uv，过滤 enterFrom=4

输出指标：
1. 非在期会员活跃 uv
2. 非在期会员首页曝光 uv
3. 能量耗尽弹窗曝光 uv
4. 耗尽弹窗订阅 CTA 展示 uv
5. 耗尽弹窗订阅按钮点击 uv
6. 订阅页展示 uv
7. 订阅页按钮点击 uv
8. 订阅成功 uv

输出转化率：
1. 耗尽弹窗曝光率 = KDM_009 uv / 非在期会员首页曝光 uv
2. 耗尽弹窗订阅点击率 = KDM_011 uv / KDM_009 uv
3. 订阅页到达率 = KDS_001 enterFrom=4 uv / KDM_011 uv
4. 订阅成功率（对耗尽曝光）= KDT_001 enterFrom=4 uv / KDM_009 uv
5. 订阅成功率（对订阅点击）= KDT_001 enterFrom=4 uv / KDM_011 uv

请注意：
如果 enterFrom=4 和 KDM_009 用户无法完全对齐，请说明归因差异。
```

## 8. 群内机器人提数请求 5：耗尽用户 vs 未耗尽用户订阅对比

这条用于判断相关性，但不能单独证明因果。

```text
@新业务数据助手 请帮我比较 2026-06-11 到 2026-06-17 Pingo 新版本非在期会员中，能量耗尽用户和未耗尽用户的订阅转化差异。

用户范围：
非在期会员、新版本用户，按 dt、user_type(new/old)、os 拆分。

分组：
1. 耗尽用户：当天有 KDM_009 能量耗尽弹窗曝光。
2. 未耗尽学习用户：当天有有效学习行为，但没有 KDM_009。
3. 未耗尽活跃用户：当天活跃，但没有 KDM_009。

输出指标：
1. 各组 uv
2. 各组订阅页展示 uv
3. 各组订阅按钮点击 uv
4. 各组订阅成功 uv
5. 各组订阅成功率
6. 各组当天学习 level 数均值
7. 各组当天答题数均值
8. 各组次日留存率

注意：
这个分析是相关性对比，不代表能量耗尽造成订阅转化提升。耗尽用户本身可能学习意愿更强，需要用学习 level 数、答题数、新老用户等维度控制。
```

## 9. 群内机器人提数请求 6：校验 scene 字段

这条建议在做分场景订阅转化前先跑。

```text
@新业务数据助手 请帮我校验 Pingo 能量耗尽弹窗相关事件的 scene 字段，时间范围 2026-06-11 到 2026-06-17。

事件范围：
1. KDM_009 能量耗尽弹窗曝光
2. KDM_010 能量耗尽弹窗订阅 CTA 展示
3. KDM_011 能量耗尽弹窗订阅按钮点击
4. KDM_014 能量耗尽弹窗退出按钮点击

输出：
1. event_name + scene 的 pv、uv 分布
2. event_name + scene + isNew 的 pv、uv 分布
3. scene 为空、非 1-6、异常值的 pv、uv
4. KDM_009、KDM_010、KDM_011、KDM_014 四个事件的 scene 分布是否一致

重点检查：
scene=3/4 是否只代表“新手引导流程触发”，不要和 isNew=1 新手期混用。
```

## 10. 建议执行顺序

按这个顺序发给机器人：

1. **每日能量收支总表**：先确认每天消耗、补充、手动补充。
2. **手动补充明细**：拆方式 A、方式 B。
3. **能量供需分布**：判断供给是否偏宽。
4. **scene 字段校验**：先确认分场景是否可信。
5. **耗尽到订阅漏斗**：看耗尽后的订阅转化。
6. **耗尽用户 vs 未耗尽用户对比**：看相关性，但不直接做因果结论。

## 11. 拿到结果后怎么判断

| 观察结果 | 可能解释 | 下一步 |
|---|---|---|
| 人均补充能量接近或高于人均消耗能量 | 能量供给可能偏宽 | 拆来源，看是方式 A、方式 B 还是奖励贡献最大 |
| 手动补充能量主要来自方式 B | 耗尽弹窗救济强 | 看 B 后是否订阅或流失 |
| 耗尽用户订阅率高于未耗尽用户 | 耗尽与订阅有正相关 | 控制学习强度后再看，避免高意愿用户偏差 |
| 耗尽用户订阅率低但 B 领取率高 | 免费补能可能替代付费 | 考虑收紧 B 或优化订阅 CTA |
| 耗尽用户流失率高 | 能量墙可能伤害体验 | 分入口/做题中/新手引导场景看 |
| 自动恢复占补充大头 | 被动供给较强 | 再看用户日末余额和耗尽频率 |
