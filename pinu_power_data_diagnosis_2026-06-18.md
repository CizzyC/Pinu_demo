# Pinu 能量系统数据分析诊断报告

更新日期：2026-06-18

输入材料：

- `/Users/admini/Downloads/埋点列表-V1.2.0.md`
- `/Users/admini/Downloads/非会员能量耗尽率_DAU→能量耗尽漏斗数据_柴智元_chaizhiyuan_20260618164202.xlsx`
- `/Users/admini/Downloads/非会员能量耗尽率_能量耗尽用户占比=能量耗尽uv_非在期会员首页曝光uv_柴智元_chaizhiyuan_20260618164126.xlsx`
- `/Users/admini/Downloads/已评审-【功能需求】V1.2.0-能量（Power）.md`

## 0. 优先诊断：用户每天“应获得多少能量” vs “实际获得多少能量”

### 0.1 先给结论

当前规则下，非订阅用户每天可获得的能量不是一个固定值，而是由“是否消耗到未满、是否进入能量页领取方式 A、是否耗尽触发方式 B、是否答得好触发奖励”共同决定。

最关键的理论值：

```text
非新手期固定理论最大日获得能量 = 自动恢复 24 + 方式 A 120 + 方式 B 15 = 159 点/天

若计入日初满额余额：
非新手期当天理论最大可使用能量 = 日初余额 25 + 当天获得 159 = 184 点/天
```

这里要区分两个口径：

| 口径 | 数值 | 含义 |
|---|---:|---|
| 当天最多获得能量 | 159 点/天 | 只计算当天通过自动恢复、方式 A、方式 B 新增获得的能量，不含连对/全对奖励 |
| 当天最多可使用能量 | 184 点/天 | 日初满能量 25 + 当天最多获得 159，可理解为当天最多可支撑的答题消耗上限，不含连对/全对奖励 |

这个理论值只适用于**非新手期**。新手期因为方式 B 默认“每次充满至 25 且每日不限次”，不存在固定理论日上限；连对/全对奖励也取决于当天完成 level 数、正确率和封顶截断，需单独用实际埋点统计。

如果先按“平均一个 level 有 12 题”做估算，则每个 level 理论最多可触发 3 个奖励：

| 触发点 | 奖励 | 12 题 level 是否可触发 |
|---|---:|---|
| 连对 5 | 最多 +5 | 可以 |
| 连对 10 | 最多 +5 | 可以 |
| 整卷全对 | 最多 +5 | 可以 |
| 合计 | 最多 +15/level | 可以 |

因此，在“每个 level 12 题、用户全对、奖励都抽到 +5、且不被封顶截断”的理想情况下：

```text
单 level 理论最大奖励 = 15 点
单 level 题目消耗 = 12 点
单 level 理论净补能 = 15 - 12 = +3 点
```

这意味着：如果用户能持续全对，奖励本身就可能覆盖甚至超过该 level 的题目消耗。实际产品中会受到正确率、题量、发奖随机值、余额封顶截断、内容量和学习时长影响，所以不能直接把它当成真实可得能量。

结合现有“2026-06-11 到 2026-06-17 学习用户人均约 3.16 个 level/天”的数据，可以得到一个平均行为下的奖励上限估算：

```text
平均学习用户每日理论最大奖励能量 ≈ 3.16 level * 15 = 47.4 点/天

非新手期固定理论最大日获得能量 + 平均行为奖励上限
= 159 + 47.4
= 206.4 点/天

若再计入日初满额余额：
25 + 206.4 = 231.4 点/天
```

这个 `206.4 点/天` 不是用户真实平均获得值，而是“按当前平均学习 level 数倒推的奖励上限估算”。真实值仍需要用 `KDM_008` 的 `rewardAmount` 和发奖前余额来验证。

从规则推导：

| 用户类型 / 场景 | 每日理论可获得能量 | 说明 |
|---|---:|---|
| 只靠自动恢复 | 最多 24 点/自然日 | 每 60 分钟 +1，满值停止；只有余额低于上限时才恢复 |
| 非新手期，自动恢复 + 方式 B | 最多 39 点/自然日，不含方式 A 和奖励 | 自动恢复 24 + 方式 B 3 次 * 5 = 15 |
| 非新手期，自动恢复 + 方式 A + 方式 B | 理论最多 159 点/自然日，不含奖励 | 自动恢复 24 + 方式 A 24 次 * 5 + 方式 B 15；这是“极限行为”口径，不代表真实用户 |
| 新手期方式 B | 理论上不适合给固定日上限 | 方式 B 默认每次充满至 25 且每日不限次，实际获得量取决于当天耗尽次数 |
| 连对 / 全对奖励 | 不固定 | 每个 level 内连对 5、连对 10、整卷全对各有规则，单次 +1 到 +5，且受上限截断 |

所以，建议把“期望用户每天获得能量值”拆成两个口径：

1. **规则供给上限**：系统理论上允许用户获得多少能量。
2. **实际获得均值**：真实用户每天通过自动恢复、方式 A、方式 B、奖励分别拿到了多少能量。

目前两份前期 Excel 只有 DAU、首页曝光、耗尽 UV 和耗尽率，**不能直接计算平均实际用户每天获得能量值**。要得到这个值，必须补查能量来源流水或对应埋点。

### 0.2 推荐的实际获得能量口径

建议数据机器人优先产出用户日级宽表：

```text
用户每日实际获得能量 =
自动恢复获得能量
+ 方式A领取成功获得能量
+ 方式B领取成功获得能量
+ 连对奖励获得能量
+ 全对奖励获得能量
```

建议同时输出：

| 指标 | 口径 |
|---|---|
| 人均每日实际获得能量 | 每日实际获得总能量 / 非订阅活跃用户 UV |
| 学习用户人均每日实际获得能量 | 每日实际获得总能量 / 当日有效答题用户 UV |
| 耗尽用户人均每日实际获得能量 | 耗尽用户当日实际获得总能量 / 耗尽用户 UV |
| 各来源能量占比 | 某来源获得能量 / 总获得能量 |
| 人均每日消耗能量 | 有效答题扣减总能量 / 非订阅活跃用户 UV |
| 净能量压力 | 人均每日消耗能量 - 人均每日实际获得能量 |

### 0.3 用于发给数据机器人的第一条请求

可以直接发：

```text
@新业务数据助手 请帮我查询 Pingo 能量系统中，2026-06-11 到 2026-06-17 非在期会员用户每天实际获得能量的来源拆分。

口径要求：
1. 用户范围：非在期会员，按 user_type(new/old)、os 拆分；如果有 app 版本，请保留新版本归属口径。
2. 统计粒度：dt + user_type + os。
3. 指标：
   - 非在期会员活跃 uv
   - 有效答题用户 uv
   - 能量耗尽 uv
   - 自动恢复获得能量总量
   - 方式A领取成功获得能量总量、领取成功 uv、领取成功次数
   - 方式B领取成功获得能量总量、领取成功 uv、领取成功次数
   - 连对奖励获得能量总量、触发 uv、触发次数
   - 全对奖励获得能量总量、触发 uv、触发次数
   - 有效答题消耗能量总量
   - 人均每日实际获得能量 = 获得能量总量 / 非在期会员活跃 uv
   - 学习用户人均每日实际获得能量 = 获得能量总量 / 有效答题用户 uv
   - 人均每日消耗能量 = 有效答题消耗能量总量 / 非在期会员活跃 uv
4. 注意：
   - 方式A/方式B只统计 claimStatus=成功。
   - 奖励能量用实际发放 rewardAmount，不用理论随机值。
   - 如果自动恢复没有埋点，请优先查后端能量流水；如果没有流水，请说明无法统计。
```

### 0.4 可选 SQL 草稿

表名和字段名需要数据机器人根据数仓实际结构替换。这里重点是口径，不是直接可运行版本。

```sql
-- 目标：非在期会员用户日级能量获得与消耗拆分
WITH active_user AS (
  SELECT
    dt,
    cuid,
    user_type,
    os
  FROM /* 活跃用户表 */
  WHERE dt BETWEEN '20260611' AND '20260617'
    AND is_subscribe = 0
),
power_gain AS (
  SELECT
    dt,
    cuid,
    CASE
      WHEN event_name = 'KDM_007' AND claimStatus = '1' THEN 'method_A'
      WHEN event_name = 'KDM_013' AND claimStatus = '1' THEN 'method_B'
      WHEN event_name = 'KDM_008' AND comboCount IN ('1','2') THEN 'combo_reward'
      WHEN event_name = 'KDM_008' AND comboCount = '3' THEN 'full_correct_reward'
      WHEN change_reason = 'auto_recover' THEN 'auto_recover'
      ELSE 'other'
    END AS gain_source,
    SUM(CAST(rewardAmount AS DOUBLE)) AS gain_power,
    COUNT(*) AS gain_cnt
  FROM /* 能量埋点表或服务端能量流水表 */
  WHERE dt BETWEEN '20260611' AND '20260617'
  GROUP BY dt, cuid,
    CASE
      WHEN event_name = 'KDM_007' AND claimStatus = '1' THEN 'method_A'
      WHEN event_name = 'KDM_013' AND claimStatus = '1' THEN 'method_B'
      WHEN event_name = 'KDM_008' AND comboCount IN ('1','2') THEN 'combo_reward'
      WHEN event_name = 'KDM_008' AND comboCount = '3' THEN 'full_correct_reward'
      WHEN change_reason = 'auto_recover' THEN 'auto_recover'
      ELSE 'other'
    END
),
question_cost AS (
  SELECT
    dt,
    cuid,
    COUNT(*) AS valid_question_cnt,
    SUM(power_deducted) AS consumed_power
  FROM /* 答题明细表或能量扣减流水表 */
  WHERE dt BETWEEN '20260611' AND '20260617'
    AND is_skip = 0
    AND is_wrong_retry = 0
  GROUP BY dt, cuid
)
SELECT
  a.dt,
  a.user_type,
  a.os,
  COUNT(DISTINCT a.cuid) AS non_sub_active_uv,
  COUNT(DISTINCT CASE WHEN q.valid_question_cnt > 0 THEN a.cuid END) AS learning_uv,
  SUM(COALESCE(q.consumed_power, 0)) AS consumed_power,
  SUM(COALESCE(g.gain_power, 0)) AS gained_power,
  SUM(CASE WHEN g.gain_source = 'auto_recover' THEN g.gain_power ELSE 0 END) AS gained_auto_recover,
  SUM(CASE WHEN g.gain_source = 'method_A' THEN g.gain_power ELSE 0 END) AS gained_method_A,
  SUM(CASE WHEN g.gain_source = 'method_B' THEN g.gain_power ELSE 0 END) AS gained_method_B,
  SUM(CASE WHEN g.gain_source = 'combo_reward' THEN g.gain_power ELSE 0 END) AS gained_combo_reward,
  SUM(CASE WHEN g.gain_source = 'full_correct_reward' THEN g.gain_power ELSE 0 END) AS gained_full_correct_reward,
  SUM(COALESCE(g.gain_power, 0)) / COUNT(DISTINCT a.cuid) AS avg_gained_power_per_active_user,
  SUM(COALESCE(q.consumed_power, 0)) / COUNT(DISTINCT a.cuid) AS avg_consumed_power_per_active_user
FROM active_user a
LEFT JOIN power_gain g
  ON a.dt = g.dt AND a.cuid = g.cuid
LEFT JOIN question_cost q
  ON a.dt = q.dt AND a.cuid = q.cuid
GROUP BY a.dt, a.user_type, a.os
ORDER BY a.dt, a.user_type, a.os;
```

## 1. 当前已有数据能支持的结论

### 1.1 两份前期数据口径一致

已校验：

| 校验项 | 结果 |
|---|---|
| `新版本渗透率 = 新版本DAU / DAU` | 通过，最大差异 0 |
| `非会员首页曝光渗透率 = 非在期会员首页曝光uv / 新版本DAU` | 通过，最大差异 0 |
| `能量耗尽用户占比 = 能量耗尽uv / 非在期会员首页曝光uv` | 通过，最大差异 0 |
| 两份 Excel 的 `能量耗尽用户占比` 是否一致 | 通过，2026-06-11 到 2026-06-17 每日 new/old 完全一致 |

这说明当前“能量耗尽用户占比”这一个指标本身计算没有明显 Excel 层面的错误。

### 1.2 iOS 是当前有效分析主体，Android 暂不能分析

2026-06-11 到 2026-06-17 汇总：

| user_type | os | DAU | 新版本DAU | 非在期会员首页曝光uv | 能量耗尽uv | 汇总耗尽率 |
|---|---|---:|---:|---:|---:|---:|
| new | iOS | 1,498 | 1,239 | 740 | 233 | 31.49% |
| old | iOS | 9,269 | 5,475 | 5,003 | 902 | 18.03% |
| new | Android | 9 | 1 | 0 | 0 | 不可算 |
| old | Android | 6 | 3 | 0 | 0 | 不可算 |

结论：

- iOS 有足够样本，可以先做趋势诊断。
- Android 样本极小且非在期会员首页曝光 UV 为 0，本阶段不要用于判断能量系统效果。

### 1.3 新用户耗尽率显著高于老用户

2026-06-11 到 2026-06-17，按每日耗尽率简单平均：

| 用户类型 | 平均耗尽率 | 最低 | 最高 |
|---|---:|---:|---:|
| new | 30.06% | 18.18% | 37.78% |
| old | 15.20% | 0.00% | 19.38% |

按 7 天汇总 UV 口径：

| 用户类型 | 汇总耗尽率 |
|---|---:|
| new iOS | 31.49% |
| old iOS | 18.03% |

初步解读：

- 新用户更容易耗尽，可能与初始学习热情、新手引导路径、首页曝光覆盖或新手保护期行为有关。
- 老用户在 6/11 的耗尽率为 0%，同时新版本 DAU 只有 8、非在期会员首页曝光 UV 只有 6，明显受新版本渗透不足影响，不适合单独解释为“老用户不耗尽”。
- 从 6/13 起，老用户新版本渗透明显提高后，耗尽率稳定在约 17%-19%，说明老用户也会触达能量墙，但弱于新用户。

### 1.4 新版本渗透会影响前几天口径

iOS 每日漏斗：

| 日期 | 用户类型 | DAU | 新版本DAU | 新版本渗透率 | 非在期会员首页曝光uv | 非会员首页曝光渗透率 | 能量耗尽uv | 耗尽率 |
|---|---|---:|---:|---:|---:|---:|---:|---:|
| 20260611 | new | 235 | 64 | 27.23% | 44 | 68.75% | 8 | 18.18% |
| 20260611 | old | 1446 | 8 | 0.55% | 6 | 75.00% | 0 | 0.00% |
| 20260612 | new | 228 | 181 | 79.39% | 116 | 64.09% | 38 | 32.76% |
| 20260612 | old | 1362 | 194 | 14.24% | 170 | 87.63% | 27 | 15.88% |
| 20260613 | new | 198 | 182 | 91.92% | 109 | 59.89% | 29 | 26.61% |
| 20260613 | old | 1343 | 926 | 68.95% | 845 | 91.25% | 152 | 17.99% |
| 20260614 | new | 233 | 224 | 96.14% | 135 | 60.27% | 51 | 37.78% |
| 20260614 | old | 1231 | 1004 | 81.56% | 930 | 92.63% | 160 | 17.20% |
| 20260615 | new | 193 | 188 | 97.41% | 120 | 63.83% | 40 | 33.33% |
| 20260615 | old | 1262 | 1072 | 84.94% | 975 | 90.95% | 189 | 19.38% |
| 20260616 | new | 202 | 194 | 96.04% | 104 | 53.61% | 28 | 26.92% |
| 20260616 | old | 1335 | 1155 | 86.52% | 1061 | 91.86% | 202 | 19.04% |
| 20260617 | new | 209 | 206 | 98.56% | 112 | 54.37% | 39 | 34.82% |
| 20260617 | old | 1290 | 1116 | 86.51% | 1016 | 91.04% | 172 | 16.93% |

建议后续分析优先使用 2026-06-13 之后的数据，或在报告里单独标注 6/11-6/12 为新版本渗透爬坡期。

## 2. 当前口径风险和埋点缺口

### 2.1 当前耗尽率分母不是 DAU，而是“非在期会员首页曝光 UV”

当前指标：

```text
能量耗尽用户占比 = 能量耗尽uv / 非在期会员首页曝光uv
```

这个口径适合衡量“已进入新版本能量体系且首页曝光的非会员用户中，有多少耗尽”。但它不是严格的 DAU 口径。

需要避免两种误读：

- 不能直接说“30% 的新用户 DAU 耗尽”，因为分母不是 DAU。
- 不能直接代表全量非会员，因为分母经过新版本渗透、首页曝光、非在期会员等过滤。

建议后续同时产出三个分母：

| 分母 | 用途 |
|---|---|
| 非订阅 DAU | 看整体用户影响面 |
| 新版本非订阅 DAU | 看已进入新版用户的影响面 |
| 非在期会员首页曝光 UV | 看当前已曝光能量系统人群的耗尽强度 |

### 2.2 `scene` 字段存在验收风险

埋点文档里明确标注：

- `KDM_009` 能量耗尽弹窗展现：`scene不对 3和4的定义不对额，应该是新手引导流程触发的，不是新手额`
- `KDM_010` 耗尽弹窗订阅展现：`scene不对`
- `KDM_011` 耗尽弹窗订阅按钮点击：`scene不对`
- `KDM_014` 耗尽弹窗退出按钮点击：`scene不对`

影响：

- 暂时不能可靠拆分“做题中耗尽” vs “做题入口耗尽” vs “新手引导耗尽”。
- 耗尽后订阅、退出、流失分析会受场景归因影响。
- 需要先让数据机器人查 `scene` 分布和异常值，再决定是否按 scene 分析。

### 2.3 现有埋点不能直接还原自动恢复

能量埋点里有：

- 方式 A：`KDM_006` 展示、`KDM_007` 点击
- 方式 B：`KDM_012` 展示、`KDM_013` 点击
- 奖励：`KDM_008`
- 顶栏/能量页余额展示：`KDM_001/KDM_003`

但自动恢复是系统状态变化，不一定有事件。若没有后端能量流水，仅靠前端曝光点难以准确计算“自动恢复获得多少能量”。

建议优先找后端能量流水表，至少需要字段：

- `dt`
- `cuid / uid`
- `change_time`
- `before_balance`
- `after_balance`
- `change_value`
- `change_reason`

### 2.4 方式 A / B 的“点击”不等于“成功领取”

埋点中：

- `KDM_007` 方式 A 点击，带 `claimStatus`
- `KDM_013` 方式 B 点击，带 `claimStatus`

分析领取成功必须过滤：

```text
claimStatus = 1 成功
```

不能只用点击数作为获得能量数。

## 3. 埋点可回答的问题

| 分析问题 | 可用事件 | 当前判断 |
|---|---|---|
| 能量系统曝光 | `KDM_001`, `KDM_003` | 可以做顶栏/能量页曝光 |
| 方式 A 使用 | `KDM_006`, `KDM_007` | 可以做展示、点击、成功率；需用 `claimStatus` |
| 连对/全对奖励 | `KDM_008` | 可以看触发次数和 `rewardAmount` |
| 耗尽弹窗曝光 | `KDM_009` | 可以做曝光 UV；分场景需先校验 `scene` |
| 耗尽弹窗订阅点击 | `KDM_010`, `KDM_011` | 可以做订阅 CTA 展示/点击；分场景有风险 |
| 方式 B 使用 | `KDM_012`, `KDM_013` | 可以做展示、点击、成功率；需用 `claimStatus` |
| 耗尽弹窗退出 | `KDM_014` | 可以看退出点击；分场景有风险 |
| 订阅页到达与成功 | `KDS_001`, `KDS_003`, `KDT_001` | 可通过 `enterFrom=4 能量耗尽弹窗` 做归因 |
| 实际每日获得能量 | 方式 A/B/奖励部分可算，自动恢复需后端流水 | 当前埋点不完整 |
| 每日消耗能量 | 需要答题扣减事件或后端扣减流水 | 当前材料未提供完整事件 |

## 4. 下一轮数据机器人提数清单

### 4.1 最高优先级：实际每日获得能量

见本报告第 0.3 节。建议优先发这一条，因为它直接回答“能量是不是太充足”。

### 4.2 耗尽到订阅完整漏斗

```text
@新业务数据助手 请查询 2026-06-11 到 2026-06-17 Pingo 非在期会员能量耗尽到订阅的完整漏斗。

口径：
1. 用户范围：非在期会员；按 dt、user_type(new/old)、os 拆分。
2. 版本口径：同当前漏斗，使用用户最近一次活跃上报的 app 版本归属；只看新版本用户。
3. 指标：
   - 新版本非在期会员活跃 uv
   - 非在期会员首页曝光 uv
   - KDM_009 能量耗尽弹窗曝光 uv
   - KDM_010 耗尽弹窗订阅 CTA 展示 uv
   - KDM_011 耗尽弹窗订阅按钮点击 uv
   - KDS_001 订阅落地页展示 uv，其中 enterFrom=4
   - KDS_003 订阅按钮点击 uv，其中 enterFrom=4
   - KDT_001 订阅成功展示 uv，其中 enterFrom=4
4. 输出各环节转化率：
   - 耗尽弹窗曝光率 = KDM_009 uv / 非在期会员首页曝光 uv
   - 订阅按钮点击率 = KDM_011 uv / KDM_009 uv
   - 订阅页到达率 = KDS_001 enterFrom=4 uv / KDM_011 uv
   - 订阅成功率 = KDT_001 enterFrom=4 uv / KDM_009 uv
```

### 4.3 校验 `scene` 字段

```text
@新业务数据助手 请帮我检查 2026-06-11 到 2026-06-17 Pingo 能量耗尽弹窗相关事件的 scene 字段分布和异常。

事件：
- KDM_009 能量耗尽弹窗展现
- KDM_010 能量耗尽弹窗订阅展现
- KDM_011 能量耗尽弹窗订阅按钮点击
- KDM_014 能量耗尽弹窗退出按钮点击

输出：
1. 按 event_name + scene 分布 pv、uv。
2. 按 event_name + scene + isNew 分布 pv、uv。
3. 检查 scene=3/4 是否只出现在新手引导流程，而不是所有新手期用户。
4. 如有 scene 为空、非 1-6、或各事件 scene 分布明显不一致，请单独列出。
```

### 4.4 免费补能是否替代订阅

```text
@新业务数据助手 请查询 2026-06-11 到 2026-06-17 Pingo 非在期会员能量耗尽后，方式B领取与订阅的关系。

口径：
1. 用户范围：非在期会员，新版本用户。
2. 以 KDM_009 能量耗尽弹窗曝光用户为分母。
3. 按 dt、user_type、os、isNew 拆分。
4. 指标：
   - 耗尽弹窗曝光 uv
   - KDM_013 方式B领取点击 uv
   - KDM_013 claimStatus=成功 uv
   - 方式B领取成功后 10 分钟内继续答题 uv
   - 方式B领取成功后当日再次耗尽 uv
   - 方式B领取成功后当日订阅成功 uv
   - 未领取方式B但点击订阅 uv
   - 未领取方式B且当日流失 uv
```

### 4.5 体验护栏：耗尽后是否流失

```text
@新业务数据助手 请查询 2026-06-11 到 2026-06-17 Pingo 新版本非在期会员用户能量耗尽后的学习行为。

口径：
1. 以 KDM_009 能量耗尽弹窗曝光用户为分母。
2. 按 dt、user_type、os、isNew 拆分。
3. 指标：
   - 耗尽弹窗曝光 uv
   - 耗尽后 10 分钟内继续提交题目 uv
   - 耗尽后 10 分钟内完成 level uv
   - 耗尽后点击退出 KDM_014 uv
   - 耗尽后当日不再学习 uv
   - 耗尽用户次日留存 uv
   - 未耗尽学习用户次日留存 uv
```

## 5. 现阶段建议

### 5.1 不建议现在直接调参

现有数据只能证明“新/老用户中已有一部分触达耗尽”，还不能证明：

- 用户每天实际获得多少能量。
- 免费补能是否替代订阅。
- 耗尽后用户是继续学习、订阅还是流失。
- 方式 A / B / 奖励分别贡献了多少能量。

因此，不建议仅凭当前 30% 左右的新用户耗尽率就判断能量太松或太紧。

### 5.2 当前最值得追的判断

优先顺序：

1. **实际每日获得能量 vs 每日消耗能量**：判断供给是否偏宽。
2. **耗尽弹窗到订阅成功漏斗**：判断付费墙是否有效。
3. **方式 B 领取后是否订阅或流失**：判断免费救济是否替代付费。
4. **耗尽后留存和 level 完成率**：判断是否伤害学习体验。

### 5.3 初步业务判断

当前新用户耗尽率高于老用户，说明能量墙对新用户更容易发生作用。但这不一定是好事：

- 如果新用户耗尽后继续学或订阅，说明能量墙有效。
- 如果新用户耗尽后流失，说明新手期或入口场景可能需要保护。
- 如果新用户耗尽后大量领取方式 B 且不订阅，说明方式 B 可能在替代付费。

下一步必须把“耗尽”之后的行为链路补齐。
